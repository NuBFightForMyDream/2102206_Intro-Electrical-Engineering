%% User-Defined Functions

% Ex : A = cos(x) % A = result , cos = fn. name , x = input

% Start defining function
% (written in another file)

% function output = poly(x) % define poly function 
    % parameter (input) = x
    % return = output (value)
% output = 3 * x.^3 + 5 * x .^2 - 2 * x .^1 

% MUST save fn with same name